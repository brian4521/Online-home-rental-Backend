<div class="mt-3">
  <form action="/favourites" method="post">
    <button
      type="submit"
      class="bg-orange-500 px-4 py-2 rounded hover:bg-yellow-600"
    >
      Add to favourite
    </button>
    <input type="hidden" name="id" value="<%= foundHome._id %> " />
  </form>
</div>
